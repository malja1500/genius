export interface LoginFormInterface {
  type: string;
  label: string;
  name: string;
  id: string;
  placeholder: string;
  className?: string;
  wrapperClassName?: string;
  isPassword?: boolean;
  isCheckbox?: boolean;
}
